# PROMPT

write an article with code snippets for all steps on how to create a simple VueJS TODO's web app with create, edit and delete functions using composition API and Vite, and host on a AWS serverless architecture containing IAM, S3, APIGateway, Lambda Functions, DynamoDB and CloudWatch Logs. All the services provisioning must use Terraform.
Code snippets provisioning commands must to give instructions to use the aws commands in localstack (with Docker).
The VueJS TODO's web app must be hosted on a S3 bucked, read and write from dynamoDB using the APIGateway and Lambda Functions.
